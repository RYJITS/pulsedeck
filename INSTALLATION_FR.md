# Installation - pulsedeck

```powershell
git clone https://github.com/RYJITS/pulsedeck.git
cd pulsedeck
```

Aucun `package.json` detecte. Consulter le `README.md` et la fiche projet pour les pre-requis et l'utilisation.
